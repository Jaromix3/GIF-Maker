# GIF-Maker
Desktop application in C for creating and displaying frame-by-frame animations. Have a custom Linked List to dynamically store and manage the frames. The project uses the Raylib library to render the images on screen, and includes basic file reading/writing capabilities to save and load projects using string parsing.
